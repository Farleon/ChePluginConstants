wget https://launchpad.net/juju/2.3/2.3.5/+download/juju-2.3.5-centos7.tar.gz && tar xzvf $(ls juju*.tar.gz) && rm $(ls juju*.tar.gz) && echo 'export PATH="'$PWD'/juju-bin:$PATH"' >> ~/.bashrc && source ~/.bashrc && juju status

